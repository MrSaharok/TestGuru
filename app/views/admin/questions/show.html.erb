<p><%= @question.body %></p>

<h2>><%= t('.answers_header')%></h2>

<%= link_to t('.add_answer'), new_admin_question_answer_path(@question) %>

<ol>
  <%= render @question.answers %>
</ol>

<%= link_to t('admin.actions.edit'), edit_admin_question_path(@question) %> |
<%= link_to t('general.back'), admin_test_path(@question.test) %>
