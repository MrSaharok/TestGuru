<nav class="navbar navbar-light bg-light p-2"><%= link_to 'TestGuru', root_path, class: 'navbar-brand' %>
  <div>
    <% if user_signed_in? %>
      <p><%= t('general.welcome', first_name: current_user.first_name) %> |
        <% if current_user.is_a?(Admin) %>
          <%= link_to t('general.management'), admin_tests_path %> |
        <% end %>
        <%= link_to t('general.logout'), destroy_user_session_path, data: { turbo_method: :delete } %>
      </p>
    <% else %>
      <%= link_to t('general.login'), new_user_session_path %>
      <%= link_to t('general.signup'), new_user_registration_path %>
    <% end %>
  </div>
</nav>
